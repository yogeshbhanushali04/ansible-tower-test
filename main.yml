---
# tasks file for hygieia

  - name: run api jar
    hosts: all
    tasks:
      - name: just a sample hello world 
        command: echo "Hello world"; 
        command: date;
        command: hostname -i
