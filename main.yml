---
# tasks file for hygieia

  - name: run api jar
    hosts: 
    tasks:
      - name: just a sample hello world 
        shell: |
        echo "Hello world";
        date
        id 
        echo "EOF"
